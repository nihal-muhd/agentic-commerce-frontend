import { PageShell } from "@/components/page-shell";

export default function AdminAgentTracesPage() {
  return (
    <PageShell
      eyebrow="Agent observability"
      title="Agent Traces"
      description="Agent trace route placeholder. Trace storage and inspection UI will be added after the AI workflow exists."
    />
  );
}
