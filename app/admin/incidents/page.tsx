import { PageShell } from "@/components/page-shell";

export default function AdminIncidentsPage() {
  return (
    <PageShell
      eyebrow="Incident response"
      title="Incidents"
      description="Incident list route placeholder. Incident APIs and live investigation views will be added in later phases."
    />
  );
}
