import { PageShell } from "@/components/page-shell";

export default function AdminPage() {
  return (
    <PageShell
      eyebrow="Admin"
      title="Admin Dashboard"
      description="Admin landing route placeholder. Protected admin workflows will be added after authentication."
    />
  );
}
