import { PageShell } from "@/components/page-shell";

export default function CheckoutPage() {
  return (
    <PageShell
      eyebrow="Shopping flow"
      title="Checkout"
      description="Checkout route placeholder. Order creation will be added after cart and order services are ready."
    />
  );
}
